# PWA mode — Progressive Web App
VITE_APP_TARGET=pwa
# Cloud backend (Render) — the deployed PWA talks to the cloud API + WebSockets
VITE_BACKEND_URL=https://project-aeris.onrender.com
VITE_API_URL=https://project-aeris.onrender.com
VITE_WS_BACKEND_URL=wss://project-aeris.onrender.com
# AI service for local dev; on the deployed site /ai is proxied server-side
VITE_AI_URL=http://localhost:8005