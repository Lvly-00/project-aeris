# Development requirements — extend base.
-r base.txt

# Database — SQLite is built into Python, no extra driver needed for dev.
# Uncomment mysqlclient if you want to test against a local MySQL instance:
# mysqlclient>=2.2,<3.0

# Debug helpers (optional — uncomment as needed)
# django-debug-toolbar>=4.0,<5.0
# ipython>=8.0,<9.0

# Code quality
ruff>=0.4,<1.0
