# AI service requirements — development (range pins).
# For production Docker builds use: pip install -r requirements.production.txt
#
# Note: opencv-contrib-python is not currently installed; opencv-python covers
# all features used by the pipeline. Install contrib only if specific extra
# modules are needed (e.g. SIFT, SURF).

opencv-python>=4.10,<5.0
ultralytics>=8.3,<9.0
torch>=2.5,<3.0
torchvision>=0.20,<1.0
numpy>=1.26
requests>=2.32,<3.0
Pillow>=11.0,<13.0
fastapi>=0.115,<1.0
uvicorn>=0.34,<1.0
python-multipart>=0.0.9
python-decouple>=3.8,<4.0
psutil>=5.9,<8.0
